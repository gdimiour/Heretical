use notan::prelude::*;
use crate::Draw;

use crate::tiles::Block;
use crate::world::Player;
use crate::entities::Entity;

pub fn renderblocks(renderblocks: Vec<Block>, draw: &mut Draw) {

}

pub fn renderplayer(renderblocks: Player, draw: &mut Draw) {

}

pub fn renderentities(renderblocks: Vec<Entity>, draw: &mut Draw) {

}