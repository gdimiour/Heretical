use notan::prelude::*;
use crate::Draw;

use crate::tiles::Block;
use crate::player::Player;
use crate::entities::Entity;

pub fn renderblock(renderblocks: &Block, draw: &mut Draw, assets: &[Texture; 1]) {

}

pub fn renderplayer(renderblocks: Player, draw: &mut Draw) {

}

pub fn renderentity(renderblocks: Entity, draw: &mut Draw) {

}